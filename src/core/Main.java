package core;

import patterns.patternsdemonstrator.*;

public class Main {

    public static void main(String[] args) {

        // 1) shows, how delegate pattern works
        //(new DelegateDemonstrator()).Run();

        // 2) shows, how demonstrator pattern works
        //(new DecoratorDemonstrator()).Run();

        // 3) shows, how adapter pattern works
        //(new AdapterDemonstrator()).Run();

        // 4) shows, how prototype pattern works
        //(new PrototypeDemonstrator()).Run();

        // 5) shows, how object pool pattern works
        //(new ObjectPoolDemonstrator()).Run();

        // 6) shows, how singleton pattern works
        //(new SingletonDemonstrator()).Run();

        // 7) shows, how observer pattern works
        //(new ObserverDemonstrator()).Run();

        // 8) shows, how state pattern works
        //(new StateDemonstrator()).Run();

        // 9) shows, how chain of responsibility pattern works
        //(new ChainResponsibilityDemonstrator()).Run();

        // 10) shows, how  pattern works
        //(new FacadeDemonstrator()).Run();

        // 11) shows, how  pattern works
        //(new BridgeDemonstrator()).Run();

        // 12) shows, how  pattern works
        //(new StateDemonstrator).Run();










        System.out.println("end");
    }
}
